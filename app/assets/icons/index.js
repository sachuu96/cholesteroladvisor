import Icon from "./icon";

export const FoodIntakeIcon = style => {
  return Icon(require("./food-intake.png"), style);
};

export const ArrowBackIcon = style => {
  return Icon(require("./arrow-back.png"), style);
};

export const CancelIcon = style => {
  return Icon(require("./cancel.png"), style);
};

export const CheckIcon = style => {
  return Icon(require("./checked.png"), style);
};

export const PlusIcon = style => {
  return Icon(require("./plus.png"), style);
};

export const FoodScanIcon = style => {
  return Icon(require("./food-scan.png"));
};

export const CameraIcon = style => {
  return Icon(require("./camera.png"));
};

export const PhotoIcon = style => {
  return Icon(require("./photo.png"));
};

export const One = style => {
  return Icon(require("./one.png"));
};

export const Two = style => {
  return Icon(require("./two.png"));
};

export const Three = style => {
  return Icon(require("./three.png"));
};
